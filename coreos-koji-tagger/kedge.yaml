name: coreos-koji-tagger

deploymentConfigs:
  - containers:
    - image: ""
#      envFrom:
#      - secretRef:
#          name: pagure-token
    triggers:
      - type: ImageChange
        imageChangeParams:
          automatic: true
          containerNames:
          - coreos-koji-tagger
          from:
            kind: ImageStreamTag
            name: coreos-koji-tagger-img:latest
#secrets:
#  - name: pagure-token
#    data:
#      PAGURE_TOKEN: [[ PAGURE_TOKEN ]]
